# 配置zk
storm.zookeeper.servers:
  - "qc"
# 配置目录
storm.local.dir: "/var/storm"
# 配置nimbus leader
nimbus.seeds: ["master"]
# 配置从节点的工作端口
supervisor.slots.ports:
  - 6700
  - 6701
  - 6702
  - 6703

# 配置drpc svr
drpc.servers:
  - "master"
